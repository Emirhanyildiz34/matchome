name: match_home
description: "MatchHome - Ev arkadaşı ve kiralık ev bulma uygulaması"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.3.6
  flutter_riverpod: ^2.5.1
  go_router: ^14.0.0
  cupertino_icons: ^1.0.6
  image_picker: ^1.0.0
  url_launcher: ^6.3.0
  speech_to_text: ^7.0.0
  geolocator: ^13.0.0
  http: ^1.2.0
  sign_in_button: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
